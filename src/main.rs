fn main() {
    println!("Hello, world!");
}

// cargo new - create a new project
// cargo build - runs compile and makes executable
// cargo check - runs compile but doesn't make executable
// cargo run - does the compile/build if necessary and then run

// cargo build --release : compiles with opitimizations for prod
